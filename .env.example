# Create an app at https://developer.whoop.com and copy these in.
WHOOP_CLIENT_ID=your_whoop_client_id
WHOOP_CLIENT_SECRET=your_whoop_client_secret
# Must match the redirect URI registered in your WHOOP app exactly:
WHOOP_REDIRECT_URI=https://your-site.netlify.app/api/whoop/callback
